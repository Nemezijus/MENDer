/**
 * Shared UI labels for split modes.
 *
 * NOTE: This is presentation-only; the backend/engine owns split behavior.
 */

export const SPLIT_MODE_OPTIONS = [
  { value: 'holdout', label: 'Hold-out' },
  { value: 'kfold', label: 'K-fold cross-validation' },
];
